/** Shared Framer Motion timing tokens, mirroring src/styles/tokens.css. */
export const motionDurations = {
  fast: 0.14,
  standard: 0.2,
  revealMedium: 0.38,
  revealEditorial: 0.55,
} as const;

export const motionEasing = [0.4, 0, 0.2, 1] as const;
